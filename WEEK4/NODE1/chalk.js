/*
    EXTERNAL DEPENDENCIES (NEEDS TO BE INSTALLED)
*/
const chalk = require('chalk');

console.log(chalk.red('This is red text'));
console.log(chalk.green.bold('This is bold green text'));
// That we are from SIT