
//using setTimeout
while(1){
    //setTimeout calls the function after 1s
    /*this infinite loop will call the setTimeout function after each execution cycle
        which will print the incrementing values of natural numbers, in a stopwatch fashion
    */
    setTimeout(callback, 1000)
}